import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "agent-replay — watch a Claude agent's tool-calling loop",
  description:
    "Paste a Claude agent trace and replay it step by step — every tool call, result, and decision, on a cinematic timeline. No key, no backend.",
  openGraph: {
    title: "agent-replay",
    description: "Replay an AI agent's tool-calling loop, step by step.",
    type: "website",
  },
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
      <body className="min-h-full">{children}</body>
    </html>
  );
}
